import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF7C3AED); // Purple
  static const Color secondary = Color(0xFFA78BFA); // Light Purple
  static const Color backgroundDark = Color(0xFF0B1020);
  static const Color surfaceDark = Color(0xFF1B2339); // Slightly lighter than background
  static const Color textPrimary = Color(0xEBFFFFFF);
  static const Color textSecondary = Color(0xADFFFFFF);
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFFBBF24);
  static const Color danger = Color(0xFFEF4444);
  
  // Glassmorphism / Overlays
  static Color cardOverlay = Colors.white.withOpacity(0.06);
  static Color borderOverlay = Colors.white.withOpacity(0.10);
}

